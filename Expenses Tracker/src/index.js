import ReactDOM from 'react-dom/client';

import './index.css';
import App from './App';

//only do this once for the root component
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
